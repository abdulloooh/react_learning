npm i bootstrap@4.1.1

extensions: simple react snippets by Burke Holland
            Prettier by Esben Petersen

picsum.photos

$r
$r.render
$0
$0..click() 

single source of truth

raise event
bubble event
handle event


sfc => shortcut for stateless functional component

lifecycle hooks     //NB: lifecycle hooks can only be use on cc and not sfc

=> 1. Mounting phase
=> 2. Updating phase
=> 3. Unmounting phase

npx react-codemod rename-unsafe-lifecycles   //to remame all deprecated package(function or maybe class)